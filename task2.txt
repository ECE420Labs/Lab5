val lines = sc.textFile("data_input")
val cols = lines.map(_.split("\t"))
val leftn = cols.map(c => (c(0), c(1)))
val rightn = cols.map(c => (c(1), c(0)))
val result = (leftn union rightn).groupByKey().mapValues(_.toList)
